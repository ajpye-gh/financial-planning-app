1. save/load plan improvements.
    - change "Load Plan" to "Saved Plans"
    - add visual indication of which plan is being displayed/modified
    - if no plan selected, when loading a saved plan, prompt user to store current state (auto save) into a new plan
    - delete/trash icon for each saved plan
    - export/download icon for each saved plan
        - this will open system dialog and save the json as .pyf file. 